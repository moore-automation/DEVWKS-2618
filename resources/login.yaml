# Example structure for workshop connectivity (login.yaml).
# For live shows, replace with real pod data; credentials are gitignored in practice.
# Each entry: pod name (e.g. devwks-2618-ws1-seat1) with vpn_address, vpn_username, vpn_password.
# Optionally include torque_id for reservation systems.

- example-ws1-seat1:
    vpn_address: devnetsandbox-usw1-reservation.cisco.com:20XXX
    vpn_username: your_username
    vpn_password: your_password
- example-ws1-seat2:
    vpn_address: devnetsandbox-usw1-reservation.cisco.com:20XXX
    vpn_username: your_username
    vpn_password: your_password
